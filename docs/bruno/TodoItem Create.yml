info:
  name: TodoItem Create
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/todoitems
  body:
    type: json
    data: |-
      {
        "name": "Hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
